# Plan

brief.md를 AI가 분석하여 작성합니다. 사람이 `[x]`로 승인한 항목만 실행됩니다.

> ⚠ **실행 규칙**: `[x]` 체크된 항목만 실행 가능합니다. 미승인 항목은 절대 건드리지 않습니다.

각 이슈에는 구현에 필요한 조사·의사결정·참고사항이 함께 묶여 있습니다.

---

## 이슈 목록

<!-- [ ] = 미승인  [x] = 승인됨 (AI가 [x] 항목만 실행) -->
- [ ] ISSUE-032: AdMob 실제 ID 교체 — 출시 전 테스트 ID를 실제 ID로 교체
- [x] ISSUE-056: 시각 모드 토큰 인프라 추가 — color / grayscale 테마 토큰 + 상태 저장 ✅ 완료 (2026-03-29 21:17):
- [x] ISSUE-057: 흑백 모드 UI 적용 — 상단 토글 추가 + 앱 전반 흑백 스타일 롤아웃 ✅ 완료 (2026-03-29 21:17):
- [ ] ISSUE-058: 쉬는 시간 종료 후 자동 집중 전환 방지 — break 끝나도 idle 대기, 사용자 수동 시작
- [ ] ISSUE-059: 통계 광고 1시간 쿨다운 지속성 수정 — StatisticsScreen 연결 + localStorage 유지
- [ ] ISSUE-060: 타이머 화면에 오늘 집중 시간 표시 — TimerScreen 하단에 오늘 누적 집중 시간 추가
- [ ] ISSUE-061: 결과 공유 옵션 확장 — markdown / 텍스트 복사 선택 모달 추가
- [ ] ISSUE-062: 결과 링크 공유 — 통계 데이터 인코딩 URL 생성 + 공유 페이지
- [ ] ISSUE-063: 링크를 다른 앱으로 공유 — Web Share API + 카카오·LINE·Naver Band·브라우저 딥링크 fallback

---

## Issue Details
### ISSUE-032: AdMob 실제 ID 교체 — 출시 전 테스트 ID를 실제 ID로 교체

> 출처: ISSUE-031 해결 중 AdMob App ID 구조 파악 — 테스트 ID로 임시 운영 중, 실제 출시 전 교체 필요

- 배경:
  - 현재 코드에는 Google 공식 테스트 ID가 3곳에 적용되어 있음
  - 실제 출시 시 AdMob 콘솔에서 발급받은 ID로 교체해야 실제 광고 수익 발생
  - AdMob ID 체계:

    | ID 종류 | 형식 | 용도 |
    | ------- | ---- | ---- |
    | **App ID** | `ca-app-pub-XXXX~XXXX` | AdMob SDK 초기화용 (앱당 1개) |
    | **배너 Ad Unit ID** | `ca-app-pub-XXXX/XXXX` | 배너 광고 슬롯 (광고 종류별 1개) |
    | **전면 Ad Unit ID** | `ca-app-pub-XXXX/XXXX` | 전면 광고 슬롯 (광고 종류별 1개) |

- 구현 내용:

  **Step 1: AdMob 콘솔에서 ID 발급**

  1. [admob.google.com](https://admob.google.com) → Google 계정 로그인
  2. **앱 추가** → 앱 이름 입력 → Android 선택 → **앱 ID 확인** (`ca-app-pub-XXXX~XXXX`)
  3. **광고 단위 만들기** → 배너 광고 생성 → **배너 Ad Unit ID 확인**
  4. **광고 단위 만들기** → 전면 광고 생성 → **전면 Ad Unit ID 확인**

  **Step 2: .env 파일 생성 (프로젝트 루트)**

  ```env
  VITE_ADMOB_BANNER_ID_ANDROID=ca-app-pub-발급받은값/배너AdUnitID
  VITE_ADMOB_INTERSTITIAL_ID_ANDROID=ca-app-pub-발급받은값/전면AdUnitID
  ```

  > ⚠ `.env` 파일은 `.gitignore`에 추가 — 절대 커밋하지 않음

  **Step 3: AndroidManifest.xml App ID 교체**

  파일: `android/app/src/main/AndroidManifest.xml`

  ```xml
  <meta-data
      android:name="com.google.android.gms.ads.APPLICATION_ID"
      android:value="ca-app-pub-발급받은앱ID~숫자" />
  ```

  **Step 4: capacitor.config.ts App ID 교체**

  파일: `capacitor.config.ts`

  ```ts
  android: 'ca-app-pub-발급받은앱ID~숫자',
  ```

  **Step 5: 재빌드 및 테스트**

  ```powershell
  npx cap sync android
  npx cap build android --keystorepath upload-keystore.jks --keystorepass 'eptest$00' --keystorealias upload --keystorealiaspass 'eptest$00'
  ```

- 수정 대상 파일:
  - [ ] `.env` (신규 생성 — gitignore 추가 필수)
  - [ ] `android/app/src/main/AndroidManifest.xml` — App ID 실제 값으로 교체
  - [ ] `capacitor.config.ts` — android App ID 실제 값으로 교체
- ⚠ 참고/제약사항:
  - `BannerAd.tsx`, `InterstitialAd.tsx`의 Ad Unit ID는 `.env` 변수를 우선 읽으므로 코드 수정 불필요
  - `.env` 없으면 코드 내 테스트 ID가 자동 fallback으로 사용됨
  - AdMob 계정 승인까지 수일 소요될 수 있음 — 승인 전에는 테스트 ID 유지
- 테스트 필요: Yes — 실제 광고 표시 확인
- 완료 기준: 앱 실행 시 실제 AdMob 광고 표시 확인


---



---


### ISSUE-056: 시각 모드 토큰 인프라 추가 — color / grayscale 테마 토큰 + 상태 저장

> 출처: brief.md — 흑백 모드를 추가하되 디자인 token부터 우선 추가하고, 이후 스타일 개선까지 고려해 달라는 요청

- ⏱ 예상: AI 90분 / 시니어 45분 | 🤖 Subagent: 없음

- 배경:
  - 현재 토큰 파이프라인은 `:root` 기준 단일 컬러 세트만 생성함
  - 단순 `filter: grayscale(1)`은 차트, 강조색, 광고 placeholder, 텍스트 대비를 일괄 훼손할 수 있어 유지보수와 접근성 면에서 불리함
  - 요청대로 "token first"로 가려면 색상 의미를 semantic token으로 재구성하고, 흑백 모드 전용 변형 값을 토큰 레벨에서 제공해야 함

- 구현 내용:

  **토큰 구조 개편**

  - `src/tokens/design-tokens.json`에 시각 모드별 semantic token 추가
    - 예: `surface`, `surfaceElevated`, `textPrimary`, `textSecondary`, `accent`, `accentMuted`, `border`, `chartPrimary`
  - color / grayscale 두 세트를 같은 의미 체계로 관리

  **토큰 생성기 확장**

  - `scripts/generate-tokens.mjs`를 확장해
    - 기본 `:root`는 color 모드 변수 출력
    - `[data-visual-mode='grayscale']` 선택자에는 grayscale 변수 override 출력

  **앱 상태 추가**

  - `src/store/appStore.ts`에 `visualMode: 'color' | 'grayscale'` 및 setter 추가
  - 사용자 선택을 localStorage에 저장해 새로고침 후에도 유지
  - `src/App.tsx` 루트 컨테이너에 `data-visual-mode` 속성 연결

- 수정 대상 파일:
  - [ ] `src/tokens/design-tokens.json` — 시각 모드별 semantic token 정의
  - [ ] `scripts/generate-tokens.mjs` — grayscale override CSS 생성 로직 추가
  - [ ] `src/styles/_tokens.css` — 토큰 재생성 결과 반영
  - [ ] `src/store/appStore.ts` — visualMode 상태 및 persistence
  - [ ] `src/store/appStore.test.ts` — visualMode 변경/저장 테스트
  - [ ] `src/App.tsx` — 루트 data attribute 연결

- 참고/제약사항:
  - 기존 CSS가 일부 raw color 또는 구형 변수명에 의존하고 있으면 semantic token 쪽으로 순차 치환 필요
  - grayscale 팔레트는 완전 무채색만 고집하기보다 대비와 계층이 유지되는 "저채도 neutral" 범위까지 허용하는 것이 실사용에 유리
  - 차트/강조색도 토큰화 대상에 포함해야 후속 ISSUE-057에서 일관 적용 가능

- 테스트 필요: Yes — appStore visualMode 변경 및 초기값 복원 테스트
- 완료 기준:
  - 앱 루트에 `data-visual-mode="color|grayscale"`가 반영됨
  - 흑백 모드용 토큰이 생성 파이프라인을 통해 CSS 변수로 출력됨
  - 사용자가 시각 모드를 바꾸면 새로고침 후에도 유지됨


---



---


### ISSUE-057: 흑백 모드 UI 적용 — 상단 토글 추가 + 앱 전반 흑백 스타일 롤아웃

> 출처: brief.md — 상단 아이콘으로 칼라/흑백 모드 선택을 추가하고, 흑백 사진처럼 눈 피로가 적은 방향으로 스타일 개선 요청

- ⏱ 예상: AI 120분 / 시니어 60분 | 🤖 Subagent: 없음

- 배경:
  - ISSUE-056이 인프라라면, 실제 체감 품질은 각 화면과 컴포넌트가 semantic token을 소비하도록 바꾸는 단계에서 결정됨
  - 상단 제어는 현재 `HomeScreen`, `LoginScreen`의 `LanguageSwitcher`, `TimerScreen`의 잠금 버튼 등 화면별로 흩어져 있음
  - 흑백 모드는 단순히 "색 제거"가 아니라 카드 대비, 타이머 링, 차트, 배지, 진행바가 모두 읽히는 상태여야 함

- 구현 내용:

  **상단 토글 UI**

  - 재사용 가능한 `DisplayModeToggle` 컴포넌트 추가
  - 아이콘 버튼 1개 또는 2-state toggle로 구성하고 `aria-pressed`, `aria-label` 제공
  - `HomeScreen`, `LoginScreen`, `TimerScreen`, `StatisticsScreen`, `GoalScreen`, `MemoScreen`의 상단 헤더 영역에 배치
  - `LanguageSwitcher`와 함께 놓이는 화면은 레이아웃 정리 필요

  **스타일 롤아웃**

  - `shared.module.css`, `App.module.css` 중심으로 공용 surface / text / border / button / progress 색상을 semantic token 기반으로 치환
  - `TimerScreen`, `StatisticsScreen` 등 강조색 의존 화면은 grayscale 토큰에 맞춘 대비 조정
  - 차트 dataset 색상도 CSS 변수 또는 visualMode 분기로 흑백 팔레트 사용

  **문구 및 접근성**

  - 로케일 파일에 color/grayscale 모드 토글 라벨 추가
  - 현재 활성 모드를 스크린리더가 식별할 수 있게 처리

- 수정 대상 파일:
  - [ ] `src/components/DisplayModeToggle.tsx` 신규 — 시각 모드 토글 컴포넌트
  - [ ] `src/components/DisplayModeToggle.module.css` 신규 — 토글 스타일
  - [ ] `src/components/LanguageSwitcher.module.css` — 상단 공존 레이아웃 보정
  - [ ] `src/screens/HomeScreen.tsx`
  - [ ] `src/screens/LoginScreen.tsx`
  - [ ] `src/screens/TimerScreen.tsx`
  - [ ] `src/screens/StatisticsScreen.tsx`
  - [ ] `src/screens/GoalScreen.tsx`
  - [ ] `src/screens/MemoScreen.tsx`
  - [ ] `src/styles/shared.module.css`
  - [ ] `src/App.module.css`
  - [ ] `src/locales/ko/translation.json`
  - [ ] `src/locales/en/translation.json`
  - [ ] `src/locales/zh/translation.json`

- 참고/제약사항:
  - 흑백 모드는 `filter: grayscale(1)`로 끝내지 않고 token-driven으로 적용
  - 통계 차트는 색 차이 대신 명도 차이와 선/막대 대비로 읽혀야 함
  - 광고 placeholder, 아이콘 이모지, 언어 배지까지 완전 무채색으로 강제할지 여부는 화면 시인성을 보며 조정 필요

- 테스트 필요: Yes — 토글 상호작용, visualMode 반영, 주요 화면 렌더링 회귀 테스트
- 완료 기준:
  - 상단 아이콘 토글로 color / grayscale 모드 전환 가능
  - 주요 화면의 배경/카드/텍스트/버튼/차트가 흑백 모드에서도 읽히고 구분 가능
  - 흑백 모드에서 눈부심이 줄어든 저채도 톤으로 앱 전반이 일관되게 표시됨


---


### ISSUE-058: 쉬는 시간 종료 후 자동 집중 전환 방지 — break 끝나도 idle 대기, 사용자 수동 시작

> 출처: brief.md — "쉬는 시간 끝나고 자동으로 집중 시간으로 넘어가지 않도록 처리"

- ⏱ 예상: AI 30분 / 시니어 15분 | 🤖 Subagent: 없음

- 배경:
  - 현재 `timerStore.ts`의 `tick()`(line 196)과 `onForeground()`(line 148)에서 타이머가 0이 되면 자동으로 다음 모드로 전환하고 `status: 'running'`으로 자동 시작함
  - focus → break 자동 전환은 의도된 동작으로 유지
  - break → focus 자동 전환만 방지: 휴식이 끝나면 focus 모드로 전환하되 `status: 'idle'`로 대기해 사용자가 직접 시작 버튼을 눌러야 집중 시간이 시작되게 함

- 구현 내용:

  **`timerStore.ts` — `tick()` 수정**

  ```ts
  // 기존: nextMode 상관없이 status: 'running'으로 자동 시작
  // 변경: break → focus 전환 시 status: 'idle', sessionStart: null로 설정
  const autoStart = mode === 'focus' // focus 완료 → break는 자동 시작 유지
  set({
    mode: nextMode,
    status: autoStart ? 'running' : 'idle',
    timeLeft: getDurationForMode(nextMode),
    sessionStart: autoStart ? nextSessionStart : null,
    ...
  })
  ```

  **`timerStore.ts` — `onForeground()` 수정**

  - 동일 패턴으로 background 복귀 후 break → focus 전환 시 idle 처리

  **완료 알림 개선**

  - break 종료 후 `TimerScreen`에서 기존 `completionNotice` 또는 별도 알림으로 "휴식 완료! 집중을 시작하세요" 안내
  - 기존 focus 완료 알림과 구분해 메시지 변경

- 수정 대상 파일:
  - [ ] `src/store/timerStore.ts` — `tick()`, `onForeground()` break→focus 전환 시 idle 처리
  - [ ] `src/store/timerStore.test.ts` — break 종료 후 status가 idle인지 검증 테스트 추가
  - [ ] `src/locales/ko/translation.json` — "휴식 완료, 집중 시작 대기" 알림 문구 추가
  - [ ] `src/locales/en/translation.json`
  - [ ] `src/locales/zh/translation.json`

- 참고/제약사항:
  - focus → break 자동 전환(`mode === 'focus'` 분기)은 현행 유지 — 사용자 요청 범위 외
  - `onForeground()`에서도 동일 분기 로직 적용 필수 (background에서 break가 끝난 경우 처리)
  - `completedToday`는 focus 완료 시만 증가하므로 break 종료 로직에는 영향 없음

- 테스트 필요: Yes — break 완료 후 status === 'idle', mode === 'focus' 검증
- 완료 기준:
  - break 타이머 종료 후 focus 모드 idle 상태로 대기
  - 사용자가 시작 버튼을 눌러야만 집중 시간 카운트 시작
  - focus 완료 → break 자동 시작은 기존과 동일


---


### ISSUE-059: 통계 광고 1시간 쿨다운 지속성 수정 — StatisticsScreen 연결 + localStorage 유지

> 출처: brief.md — "통계를 볼때 광고가 한번 나오면, 다시 통계를 들어갈때 1시간동안은 광고가 나오지 않도록 처리"

- ⏱ 예상: AI 20분 / 시니어 10분 | 🤖 Subagent: 없음

- 배경:
  - `appStore.ts`에 `triggerStatisticsAd()` 함수와 `lastStatisticsAdAt: number | null` 상태가 이미 구현되어 있음 (1시간 쿨다운 로직 포함)
  - 그러나 `StatisticsScreen.tsx` line 52에서 쿨다운이 없는 `triggerInterstitial()`를 사용 중 → 매번 광고 노출
  - `lastStatisticsAdAt`은 메모리 상태만으로 저장 → 앱 재시작 시 쿨다운 초기화

- 구현 내용:

  **`StatisticsScreen.tsx` 수정**

  ```ts
  // 기존
  const { triggerInterstitial } = useAppStore()
  // ...
  triggerInterstitial()

  // 변경
  const { triggerStatisticsAd } = useAppStore()
  // ...
  triggerStatisticsAd()
  ```

  **`appStore.ts` — `lastStatisticsAdAt` localStorage 지속성 추가**

  ```ts
  const AD_COOLDOWN_KEY = 'focustimer_stats_ad_last'

  function loadLastStatisticsAdAt(): number | null {
    try {
      const stored = localStorage.getItem(AD_COOLDOWN_KEY)
      return stored ? Number(stored) : null
    } catch { return null }
  }

  // 초기값: loadLastStatisticsAdAt()
  // triggerStatisticsAd() 내부에서 set 시 localStorage.setItem(AD_COOLDOWN_KEY, now) 추가
  ```

- 수정 대상 파일:
  - [ ] `src/screens/StatisticsScreen.tsx` — `triggerInterstitial` → `triggerStatisticsAd` 교체
  - [ ] `src/store/appStore.ts` — `lastStatisticsAdAt` localStorage 저장/복원 추가
  - [ ] `src/store/appStore.test.ts` — 쿨다운 내 재호출 시 광고 미노출, 쿨다운 후 노출 테스트

- 참고/제약사항:
  - `lastStatisticsAdAt` 저장은 `triggerStatisticsAd()` 내부에서 실제 광고가 노출될 때만 수행
  - 테스트 환경에서 `localStorage` mock 처리 필요

- 테스트 필요: Yes — 쿨다운 within 1시간 재진입 시 광고 미발생, localStorage 지속성 확인
- 완료 기준:
  - 통계 화면 첫 진입 시 광고 1회 노출
  - 이후 1시간 내 재진입 시 광고 미노출
  - 앱 재시작 후에도 쿨다운 유지


---


### ISSUE-060: 타이머 화면에 오늘 집중 시간 표시 — TimerScreen 하단에 오늘 누적 집중 시간 추가

> 출처: brief.md — "하루에서 집중한 시간도 표시해줘"

- ⏱ 예상: AI 20분 / 시니어 10분 | 🤖 Subagent: 없음

- 배경:
  - `HomeScreen`에는 `todayMinutes` 카드가 있지만 `TimerScreen`에는 오늘 집중 누적 시간이 표시되지 않음
  - 사용자가 타이머 화면에서 집중하는 동안 "오늘 몇 분 집중했는지"를 확인할 수 없어 동기부여 지점이 부족함
  - `useStudyStore().getTodayMinutes()`와 `useTimerStore().completedToday` 모두 이미 타이머 화면에서 접근 가능

- 구현 내용:

  **`TimerScreen.tsx` 수정**

  - `useStudyStore`에서 `getTodayMinutes` 추가 구독
  - 타이머 링 아래 또는 세션 카운트 영역 근처에 오늘 집중 시간 요약 행 추가

  ```tsx
  const todayMinutes = getTodayMinutes()

  // UI 추가 (타이머 링 하단, pomodoroDots 근처)
  <div className={styles.todayFocusSummary}>
    <span>{t('timer.todayFocusTime')}</span>
    <strong>{formatMinutes(todayMinutes)}</strong>
  </div>
  ```

  **로케일 추가**

  - `timer.todayFocusTime`: "오늘 집중" / "Today" / "今日"

- 수정 대상 파일:
  - [ ] `src/screens/TimerScreen.tsx` — `useStudyStore` 추가, 오늘 집중 시간 UI 추가
  - [ ] `src/screens/TimerScreen.module.css` — `todayFocusSummary` 스타일 추가
  - [ ] `src/locales/ko/translation.json` — `timer.todayFocusTime` 키 추가
  - [ ] `src/locales/en/translation.json`
  - [ ] `src/locales/zh/translation.json`

- 참고/제약사항:
  - `getTodayMinutes()`는 저장된 세션 기준 → 현재 진행 중인 세션은 완료 후 반영
  - UI는 기존 타이머 영역을 해치지 않는 소형 정보 행으로 구성

- 테스트 필요: No (표시 로직은 기존 getTodayMinutes 값 출력이므로 별도 테스트 불필요)
- 완료 기준:
  - 타이머 화면에서 오늘 집중 누적 시간이 표시됨
  - 집중 세션 완료 후 값이 갱신됨


---


### ISSUE-061: 결과 공유 옵션 확장 — markdown / 텍스트 복사 선택 모달 추가

> 출처: brief.md — "결과 공유시 선택해서 통계 내용을 markdown 이나 텍스트 복사가 되도록 처리해줘"

- ⏱ 예상: AI 60분 / 시니어 30분 | 🤖 Subagent: 없음

- 배경:
  - 현재 `StatisticsScreen`의 공유 버튼은 `shareScreenshot()`으로 이미지 스크린샷만 공유 가능
  - markdown 형식(노션, GitHub, 블로그 등에 붙여넣기 용)과 plain text 복사 옵션을 추가해 다양한 공유 경로 지원

- 구현 내용:

  **`utils/share.ts` 함수 추가**

  ```ts
  export interface StudyStats {
    date: string        // YYYY-MM-DD
    todayMinutes: number
    weekTotal: number
    monthTotal: number
    totalMinutes: number
    todaySessions: number
  }

  export function generateTextReport(stats: StudyStats, t: TFunction): string {
    // 예시 출력:
    // [FocusTimer 학습 기록 - 2026-03-31]
    // 오늘: 90분 (3세션)
    // 이번 주: 420분
    // 이번 달: 1,800분
    // 누적: 12,300분
  }

  export function generateMarkdownReport(stats: StudyStats, t: TFunction): string {
    // 예시 출력:
    // ## FocusTimer 학습 기록 - 2026-03-31
    // | 기간 | 시간 |
    // |------|------|
    // | 오늘 | 90분 (3세션) |
    // | 이번 주 | 420분 |
    // ...
  }
  ```

  **공유 옵션 모달 UI**

  - `StatisticsScreen`의 공유 버튼 클릭 시 인라인 드롭다운 또는 bottom sheet 형태로 옵션 표시
  - 옵션: "텍스트 복사", "Markdown 복사", "스크린샷 공유"
  - 각 버튼 클릭 시 해당 동작 수행 후 `shareFeedback` 메시지 표시

  **로케일 추가**

  - `statistics.shareOptionText`, `statistics.shareOptionMarkdown`, `statistics.shareOptionScreenshot`

- 수정 대상 파일:
  - [ ] `src/utils/share.ts` — `generateTextReport()`, `generateMarkdownReport()` 함수 추가
  - [ ] `src/utils/share.test.ts` — 두 생성 함수 단위 테스트
  - [ ] `src/screens/StatisticsScreen.tsx` — 공유 옵션 UI 추가, 각 옵션 핸들러 연결
  - [ ] `src/screens/StatisticsScreen.module.css` — 옵션 버튼/드롭다운 스타일 추가
  - [ ] `src/locales/ko/translation.json`
  - [ ] `src/locales/en/translation.json`
  - [ ] `src/locales/zh/translation.json`

- 참고/제약사항:
  - `TFunction` 직접 주입 대신 상수 레이블로 처리하거나, `t` 없이 고정 포맷으로 구현해도 됨
  - markdown 테이블이 렌더되지 않는 앱(카카오톡 등)도 있으므로 plain text 복사가 주력 옵션
  - 스크린샷 공유는 기존 `shareScreenshot()` 재사용

- 테스트 필요: Yes — `generateTextReport`, `generateMarkdownReport` 출력 포맷 단위 테스트
- 완료 기준:
  - 공유 버튼 클릭 시 3가지 옵션 표시
  - "텍스트 복사" / "Markdown 복사" 클릭 시 클립보드에 해당 포맷 복사
  - "스크린샷 공유" 는 기존 동작 유지


---


### ISSUE-062: 결과 링크 공유 — 통계 데이터 인코딩 URL 생성 + 공유 페이지

> 출처: brief.md — "결과를 링크로 공유할수 있게 처리해줘"

- ⏱ 예상: AI 60분 / 시니어 30분 | 🤖 Subagent: 없음

- 배경:
  - 스크린샷·텍스트 복사 외에 링크로 결과를 공유하면 수신자가 브라우저에서 통계 카드를 바로 볼 수 있음
  - 별도 서버 불필요: 통계를 URL 파라미터(또는 hash)로 인코딩하고, `public/share.html` 정적 뷰어에서 렌더링
  - 앱 내 공유 URL 형식: `capacitor://localhost/share.html#today=90&week=420&...` (로컬)
  - 향후 외부 호스팅 배포 시 도메인만 교체하면 동작하도록 URL 생성 로직 분리

- 구현 내용:

  **`utils/share.ts` — `generateShareUrl()` 추가**

  ```ts
  export function generateShareUrl(stats: StudyStats): string {
    const params = new URLSearchParams({
      today: String(stats.todayMinutes),
      week: String(stats.weekTotal),
      month: String(stats.monthTotal),
      total: String(stats.totalMinutes),
      sessions: String(stats.todaySessions),
      date: stats.date,
    })
    // 환경변수 VITE_SHARE_BASE_URL이 있으면 사용, 없으면 로컬 경로
    const base = import.meta.env.VITE_SHARE_BASE_URL ?? 'capacitor://localhost/share.html'
    return `${base}?${params.toString()}`
  }
  ```

  **`public/share.html` — 정적 공유 뷰어 신규**

  - 순수 HTML/CSS (React 빌드 결과물 불필요)
  - URL search params 파싱 후 통계 카드 렌더링
  - 한국어/영어 자동 감지 (navigator.language 기준) 또는 `lang` 파라미터

  **`StatisticsScreen.tsx` — "링크 복사" 옵션 추가**

  - ISSUE-061의 공유 옵션 모달에 "링크 복사" 항목 추가
  - `generateShareUrl()` 호출 후 `copyToClipboard()` 실행

- 수정 대상 파일:
  - [ ] `src/utils/share.ts` — `generateShareUrl()` 함수 추가
  - [ ] `public/share.html` — 정적 통계 뷰어 신규 생성
  - [ ] `src/screens/StatisticsScreen.tsx` — "링크 복사" 옵션 추가 (ISSUE-061 모달에 통합)
  - [ ] `src/locales/ko/translation.json` — `statistics.shareOptionLink` 키 추가
  - [ ] `src/locales/en/translation.json`
  - [ ] `src/locales/zh/translation.json`

- 참고/제약사항:
  - `capacitor://localhost/share.html` URL은 앱 내부에서만 동작; 외부 공유 목적이면 `VITE_SHARE_BASE_URL` 환경변수로 외부 호스팅 URL 주입
  - `public/share.html`은 Vite 빌드와 독립적으로 동작하는 standalone HTML이어야 함
  - URL 파라미터는 민감 정보 없음 (집계 수치만 포함) — 별도 암호화 불필요

- 테스트 필요: Yes — `generateShareUrl()` 파라미터 포맷 단위 테스트
- 완료 기준:
  - "링크 복사" 클릭 시 통계 인코딩 URL이 클립보드에 복사됨
  - 복사된 URL을 브라우저에서 열면 `public/share.html`이 통계 카드로 렌더링됨


---


### ISSUE-063: 링크를 다른 앱으로 공유 — Web Share API + 카카오·LINE·Naver Band·브라우저 딥링크 fallback

> 출처: brief.md — "결과를 링크 주소를 다른 앱으로 공유할수 있게 처리해줘(카카오, 라인, 네이버, 브라우저 등)"

- ⏱ 예상: AI 60분 / 시니어 30분 | 🤖 Subagent: 없음

- 배경:
  - `utils/share.ts`의 `shareStudyResult()`는 이미 Web Share API(`navigator.share`)를 호출하며, Android 환경에서는 네이티브 공유 시트(인텐트 선택기)가 열려 설치된 모든 앱(카카오톡, LINE, Naver Band 등)이 나타남
  - 그러나 Capacitor webview에서 `navigator.share`가 정상 동작하는지 검증이 필요하고, 공유할 URL이 아직 없음 (ISSUE-062 선행 필요)
  - Web Share API 미지원 환경이나 사용자가 특정 앱을 바로 선택하고 싶을 경우를 위해 개별 앱 딥링크 버튼 fallback 제공

- 구현 내용:

  **`StatisticsScreen.tsx` — "앱으로 공유" 옵션 추가**

  - ISSUE-061/062 공유 모달에 "앱으로 공유" 버튼 추가
  - `shareStudyResult({ title, text, url })` 호출 (Web Share API 우선)
  - Web Share API 미지원 시 fallback 딥링크 버튼 표시

  **`utils/share.ts` — `shareToApp()` 함수 추가**

  ```ts
  export interface ShareToAppInput {
    title: string
    text: string
    url: string
  }

  export async function shareToApp(input: ShareToAppInput): Promise<ShareResult> {
    // 1차: Web Share API (Android 네이티브 공유 시트)
    if (canUseWebShare({ title: input.title, text: input.text, url: input.url })) {
      await navigator.share({ title: input.title, text: input.text, url: input.url })
      return 'shared'
    }
    // 2차: 클립보드 복사 후 fallback UI 표시
    await copyToClipboard(`${input.text}\n${input.url}`)
    return 'copied'
  }
  ```

  **딥링크 상수 및 fallback UI**

  | 앱 | URL 스킴 | 비고 |
  | --- | --- | --- |
  | KakaoTalk | `kakaotalk://send?text={encodedText}` | 설치 확인 후 호출 |
  | LINE | `line://msg/text/{encodedText}` | |
  | Naver Band | `bandapp://create/post/text?body={encodedText}` 또는 `https://band.us/share?text=...` | |
  | 브라우저 | `window.open(url, '_blank')` 또는 Capacitor Browser plugin | |

  - 딥링크 fallback은 Web Share API 미지원 시에만 노출 (인라인 버튼 4개)
  - Capacitor 환경 구분: `Capacitor.isNativePlatform()` 활용

- 수정 대상 파일:
  - [ ] `src/utils/share.ts` — `shareToApp()` 함수 및 딥링크 상수 추가
  - [ ] `src/utils/share.test.ts` — `shareToApp()` Web Share 성공/fallback 분기 테스트
  - [ ] `src/screens/StatisticsScreen.tsx` — "앱으로 공유" 옵션 및 fallback 버튼 UI
  - [ ] `src/screens/StatisticsScreen.module.css` — fallback 앱 버튼 스타일
  - [ ] `src/locales/ko/translation.json` — 앱 공유 관련 레이블 추가
  - [ ] `src/locales/en/translation.json`
  - [ ] `src/locales/zh/translation.json`

- 참고/제약사항:
  - Capacitor Android에서 `navigator.share`는 정상 동작 확인 필요 (실기기 테스트)
  - KakaoTalk 딥링크는 `kakaotalk://` 스킴이 설치 여부에 따라 오류 발생 가능 → `try/catch` 처리
  - LINE `line://msg/text/` 스킴은 LINE이 설치된 경우에만 동작
  - Naver Band는 앱 스킴 외 `https://band.us/share` 웹 fallback도 있음
  - ISSUE-062(URL 생성)가 선행되어야 `url` 파라미터를 채울 수 있음

- 테스트 필요: Yes — `shareToApp()` 분기 테스트, 딥링크 URL 포맷 검증
- 완료 기준:
  - Android에서 "앱으로 공유" 클릭 시 네이티브 공유 시트 실행
  - 카카오톡·LINE·Naver Band·브라우저 각 앱으로 공유 내용 전달 확인
  - Web Share API 미지원 환경에서 fallback 딥링크 버튼 노출
