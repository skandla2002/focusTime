# Asset Export Notes

- `timer.svg` is the source icon for browser and app branding.
- Export `timer.svg` to `icon-192.png` and `icon-512.png` when PWA icons are needed.
- `splash.svg` is the source artboard for splash-screen exports.
- Keep SVG sources in Git and generate PNG assets from these files during packaging.
